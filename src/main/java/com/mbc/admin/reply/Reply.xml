<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mbc.admin.reply.ReplyService">
	<insert id="replyinsert">
		insert into reply(replynum, movienum, replywriter, replycontent, star) 
					values(replynum_seq.nextval,
							#{param1},
							#{param2},
							#{param3},
							#{param4})
	</insert>

</mapper>
